# Folder Insight / 文件夹透视 — 产品设计文档 v0.2

> 本文档基于 PRD v1.0 + 竞品调研结论 + 设计评审意见整合。
> v0.2 主要变更：首版聚焦 Windows；收窄 MVP；明确 Treemap 展示方式；厘清 Recommendation 产品哲学；合并 File Age；移除首版 Structure 执行动作；移除首版 Action Center 和快照对比。

---

## 一、核心定位与设计原则

**一句话定位**：让用户在几秒内看清一个或多个文件夹"空间怎么被占用、结构哪里有问题、哪些内容可清理、如何安全处理"。

**设计原则**：
- 快：首屏必须快，扫描中即可浏览
- 清：从"文件视角"提升到"问题视角"，不堆砌数据
- 安全：默认保守，所有危险操作可回退
- 统一：透视 → 诊断 → 建议 → 执行，一个工作流打通

---

## 二、平台范围与版本策略

| 版本 | 平台 | 说明 |
|------|------|------|
| v1（首版） | Windows 10 / 11 | NTFS MFT 极速扫描，兼容 exFAT/FAT32 |
| v2 | macOS | jwalk + getattrlistbulk 优化 |
| v3 | Linux | jwalk 并行扫描 |

首版只交付 Windows，macOS/Linux 相关 API 和 UI 适配不在当前范围内。

---

## 三、功能模块总览

```
┌─────────────────────────────────────────────────────────────┐
│  扫描入口（多文件夹 / 多盘符统一为"文件夹集合"）              │
├──────────┬──────────┬──────────┬──────────┬─────────────────┤
│ Overview │Duplicates│ Structure│  Residue │  File Types     │
│  透视    │  重复    │  结构诊断│  残留    │  类型 + 年龄    │
├──────────┴──────────┴──────────┴──────────┴─────────────────┤
│              Insight Panel  问题摘要面板（只读，不执行）      │
├─────────────────────────────────────────────────────────────┤
│              Action Center  处理中心（v2）                   │
├─────────────────────────────────────────────────────────────┤
│              Reports  快照对比（v2）                         │
└─────────────────────────────────────────────────────────────┘

贯穿全局：扫描配置（排除规则）/ 路径搜索 / 过滤器 / 风险提示
```

---

## 三、扫描入口与多文件夹管理

### 3.1 设计决策

用户可以选择一个或多个文件夹加入同一次扫描。盘符（如 D:\）被视为该盘根目录文件夹，与普通文件夹无区别。多个文件夹合并为一个"扫描会话"，所有分析模块在这个会话范围内运行。

```
扫描会话示例：
  ├── D:\（整个 D 盘）
  └── E:\Projects\
```

### 3.2 扫描配置面板

用户在启动扫描前可配置：

**扫描目标**
- 添加文件夹（支持拖拽、路径输入、文件选择器）
- 移除已添加的文件夹
- 显示每个文件夹的文件系统类型（NTFS / exFAT / FAT32）和扫描模式（极速 / 兼容）

**排除规则**
- 内置预设排除组（可勾选启用）：
  - 开发环境：`node_modules/`、`.git/`、`.svn/`、`__pycache__/`、`target/`（Rust）、`.gradle/`
  - 虚拟机：`*.vmdk`、`*.vhd`、`*.vhdx`
  - 系统缓存：`$RECYCLE.BIN`、`System Volume Information`
- 用户自定义排除规则：
  - 支持 glob 模式（`*.log`、`temp_*/`）
  - 支持绝对路径排除
  - 规则列表可保存为命名预设，下次复用

**扫描模式提示**（只读，自动识别）
- `极速模式（NTFS MFT）`：本地 NTFS 盘 + 管理员权限时自动启用
- `兼容模式`：exFAT / FAT32 / 权限受限路径，功能不缺失，速度较慢

### 3.3 扫描过程

- 扫描中实时显示：已发现文件数、累计大小、当前扫描路径、耗时
- 边扫描边渲染：Overview Treemap 在扫描完成前即可浏览（部分结果）
- 支持取消，取消后可选择保留当前阶段结果或放弃
- 扫描完成后结果写入本地 SQLite 索引，后续模块秒开

---

## 四、Overview 透视页

### 4.1 Treemap 展示方式（参考 SpaceSniffer）

Treemap 采用**文件和文件夹混合展示**，而非只展示目录层级：

- 每个色块代表一个文件或文件夹，**面积正比于其占用空间**
- 文件夹色块内部嵌套其子内容（递归展示），体积越大的内容在视觉上越突出
- 颜色按文件类型大类区分（图片/视频/文档/代码/压缩包/其他），文件夹本身用中性色
- 扫描过程中色块实时填充，不等扫描完成

这种方式的核心优势：用户不需要逐层点击，一眼就能看到"哪个文件/文件夹最占空间"，即使它深藏在多层目录里。

### 4.2 布局

```
┌──────────────────────────────────────────────────────────┐
│  顶部概览栏：总大小 / 文件数 / 文件夹数 / 最大文件 /      │
│             最近新增大文件 / 问题数量（点击跳转 Insight） │
├────────────────────────┬─────────────────────────────────┤
│  左侧目录树            │  右侧 Treemap 主视图             │
│  + 大小列表            │                                  │
│  （可排序：大小/       │  文件和文件夹混合展示            │
│   文件数/修改时间）    │  面积 = 占用空间                 │
│                        │  颜色 = 文件类型                 │
│                        │  边扫描边增量渲染                │
├────────────────────────┴─────────────────────────────────┤
│  底部：文件类型分布条（各类型体积占比，可点击过滤 Treemap）│
└──────────────────────────────────────────────────────────┘
```

点击任意节点 → 右侧弹出详情面板（路径、子项数、大小、类型构成、最近变更、命中问题数）

### 4.3 Treemap 交互

- 双击文件夹色块：Drill-down 进入，该文件夹铺满整个 Treemap 区域
- 面包屑导航：顶部显示当前路径层级，可点击回退
- 鼠标悬停：tooltip 显示路径、大小、占比
- 右键菜单：在文件管理器中打开 / 移到回收站 / 查看重复情况
- 搜索框：支持路径、文件名、扩展名过滤，命中节点高亮

### 4.4 颜色模式切换

Treemap 支持两种着色模式，顶部切换：
- **类型模式**（默认）：按文件类型大类着色
- **年龄模式**：按最后修改时间着色，冷色（近期）→ 暖色（久远），帮助识别"僵尸目录"

年龄模式不单独作为独立模块，而是作为 Overview Treemap 的一个视角。文件年龄的详细分布数据（时间桶统计）放在 File Types 模块的"年龄"标签页中。

### 4.5 多文件夹会话展示

当扫描会话包含多个文件夹时：
- Treemap 顶层显示各文件夹作为独立根节点，按大小排列
- 左侧目录树顶层列出所有扫描根目录
- 概览栏数据为所有文件夹的汇总

---

## 五、Duplicates 重复文件模块

### 5.1 检测流程

```
文件大小分组 → xxHash 头部采样（前 4KB）→ BLAKE3 完整哈希 → 重复簇生成 → 保留建议
```

### 5.2 展示方式

- 以"重复簇"为单位展示，每簇显示：
  - 文件数量、总占用、可回收空间
  - 涉及目录列表
  - 推荐保留项（高亮标注，基于"最新修改时间"默认策略）
  - 风险标记（硬链接、只读、权限异常）
- 支持区分：同目录重复 / 跨目录重复 / 备份式重复
- 过滤条件：目录范围、文件类型、最小大小、修改时间、是否跨目录

### 5.3 保留策略

用户可选择：保留最新 / 保留最旧 / 保留指定目录优先 / 手工指定

### 5.4 执行动作（首版）

首版提供直接操作，不经过 Action Center：
- 选中副本 → 移到回收站（默认，可回退）
- 执行前展示影响预览（文件列表、释放空间）
- 执行后在当前视图标记已处理状态

不提供永久删除，不提供隔离区（v2 引入 Action Center 后统一）。

---

## 六、Structure 结构诊断模块

### 6.1 首版定位：只诊断，不执行

Structure 模块首版**只做问题发现和展示**，不提供任何文件系统操作。原因：

- 目录层级本身可能是有意义的（项目结构、相对路径依赖）
- 扁平化/合并操作会影响 git、构建系统、素材引用等
- 用户看起来只是"减少一层目录"，实际改的是路径语义
- 执行操作需要冲突检测、回退机制，复杂度高，首版不引入

首版 Structure 的价值是：**让用户看到结构问题，自己决定是否手动处理**。

### 6.2 诊断项

| 诊断项 | 说明 | 首版动作 |
|--------|------|---------|
| 空文件夹 | 完全空目录 | 展示 + 移到回收站（低风险，首版可做） |
| 单子目录链 | 连续只有一个子目录的路径链 | 仅展示，标注链长度和末端内容 |
| 深层嵌套 | 超过阈值深度的路径 | 仅展示，按深度排序 |
| 超长路径 | 接近或超过 260 字符限制 | 仅展示，显示完整路径 |
| 小文件过密 | 文件数极多但总体积小 | 仅展示，提示可能是缓存/构建产物 |
| 异常命名 | A/A/A、New Folder/New Folder | 仅展示，标记供人工确认 |

**空文件夹是唯一首版提供执行的诊断项**，因为它风险极低（空目录删除不影响任何文件），且用户预期明确。

### 6.3 展示方式

每条诊断结果包含：
- 路径上下文（父目录 + 当前节点）
- 问题描述（自然语言，不用技术术语）
- 风险等级标签（安全 / 谨慎）
- 对于单子目录链：显示链长度、末端文件数、完整路径预览

---

## 七、Residue 残留文件模块

### 7.1 设计决策：灵活匹配模式

残留识别支持两层：

**内置规则库**（开箱即用，只读）
- macOS 残留：`._*`、`.DS_Store`、`AppleDouble`
- Windows 残留：`Thumbs.db`、`desktop.ini`
- 通用临时文件：`*.tmp`、`*.temp`、部分下载残片（`.crdownload`、`.part`）

**用户自定义规则**（灵活扩展）
- 支持 glob 模式：`*.bak`、`~$*`（Office 临时文件）、`*.orig`
- 支持正则模式（高级模式，可折叠）
- 支持按路径模式匹配：仅在特定目录下生效
- 规则可命名、可导出/导入（JSON 格式）
- 规则可设置风险等级（影响默认勾选行为）

**规则管理界面**
```
┌─────────────────────────────────────────────┐
│  内置规则（只读，可启用/禁用）               │
│  ✓ macOS 残留文件                           │
│  ✓ Windows 缩略图缓存                       │
│  ✓ 通用临时文件                             │
├─────────────────────────────────────────────┤
│  自定义规则                                  │
│  + 添加规则                                  │
│  [glob] *.bak          风险：低  ✓启用       │
│  [glob] ~$*            风险：低  ✓启用       │
│  [regex] ^\.nfs.*      风险：中  ✓启用       │
└─────────────────────────────────────────────┘
```

### 7.2 展示方式

- 按规则类别分组展示，每类显示来源说明（"该文件通常由 macOS Finder 生成"）
- 支持一键移到回收站
- 系统关键目录中的同名文件默认不勾选
- 支持"排除此文件"（加入白名单，永久跳过）

---

## 八、File Types 文件类型统计模块

### 8.1 分类体系

按扩展名映射到大类：图片 / 视频 / 音频 / 文档 / 压缩包 / 安装包 / 代码 / 数据库 / 缓存 / 未知

### 8.2 展示方式

```
┌──────────────────────────────────────────────────┐
│  类型分布横向条形图（按体积排序）                 │
│  点击某类型 → 展开该类型详情                      │
├──────────────────────────────────────────────────┤
│  类型详情：                                       │
│  - 体积 / 数量 / 最大文件                         │
│  - 主要分布目录（Top 5）                          │
│  - 文件列表（可排序）                             │
│  - 移到回收站按钮（选中项）                       │
├──────────────────────────────────────────────────┤
│  年龄标签页：                                     │
│  - 按修改时间分桶（本周/本月/3月内/1年内/1-3年/3年+）│
│  - 每桶：文件数、总体积、占比                     │
│  - 点击时间桶 → 展开该范围文件列表                │
└──────────────────────────────────────────────────┘
```

文件年龄数据集中在此模块的"年龄"标签页，不单独作为独立模块。Overview Treemap 的年龄着色模式与此联动（颜色映射一致）。

- 支持扩展名映射自定义（将某扩展名归入特定大类）
- "未知类型"视图帮助发现异常文件
- 首版不提供按类型直接永久删除，只提供移到回收站

---

## 九、Insight Panel 问题摘要面板

### 9.1 产品哲学明确

**Insight Panel 是"摘要入口"，不是"执行入口"。**

两种产品哲学的区别：

| 哲学 | 描述 | 问题 |
|------|------|------|
| 摘要入口 | Insight 只展示问题摘要，用户点击后跳转到对应模块详情页处理 | 用户需要多跳一步，但对问题有完整上下文 |
| 直接执行 | Insight 卡片上直接有"一键处理"按钮 | 用户可能在没有充分了解的情况下执行，且各模块详情页变成"解释页"而非"操作页" |

**首版选择：摘要入口。** 理由：
- 首版各模块本身就是操作入口，Insight 是导航层
- 用户信任需要建立，不应在摘要层就触发批量操作
- 各模块详情页保留完整的操作能力，Insight 只是"告诉你先去哪里"

### 9.2 展示方式

扫描完成后，Insight Panel 作为侧边栏或顶部折叠区展示，按优先级排序：

```
┌─────────────────────────────────────────────────────┐
│  问题摘要                                            │
├─────────────────────────────────────────────────────┤
│  ● 重复文件  12.4 GB 可回收          [→ 查看详情]   │
│  ● macOS 残留  3.2 GB               [→ 查看详情]   │
│  ● 空文件夹  186 个                  [→ 查看详情]   │
│  ● 超长路径  23 个（可能影响兼容性） [→ 查看详情]   │
└─────────────────────────────────────────────────────┘
```

- 每条只显示一句话摘要 + 数量/体积
- 点击"查看详情"跳转到对应模块，并自动定位到相关内容
- 无问题时显示"未发现明显问题"
- 问题数量显示在 Overview 顶部概览栏，点击展开 Insight Panel

### 9.3 排序逻辑

按"可节省空间 × 操作风险倒数"排序，空间收益大且风险低的排在前面。

---

## 十、Action Center 处理中心（v2）

首版不做 Action Center。原因：

Action Center 要求"分析结果"与"后续文件系统真实变化"严格对应，否则会出现：
- 扫描时能删，执行时文件已经变了
- 建议里显示能释放 8.2 GB，实际只删了 5.4 GB
- 多模块结果汇聚后，执行顺序和依赖关系复杂（删重复文件后空目录数量变化）

首版各模块直接提供操作（移到回收站），操作范围小、风险可控、结果可预期。

v2 引入 Action Center 时需要解决：
- 执行前重新验证文件是否已变化（re-stat）
- 多模块操作的依赖顺序（先删重复，再检测空目录）
- 操作日志与撤销机制

---

## 十一、Reports 快照对比（v2）

首版不做快照对比。原因：

快照对比要求区分"本次操作导致的变化"和"用户在系统里自己改的文件"，这需要：
- 操作日志与文件系统变化的精确对应
- 快照 diff 算法（路径级别的增删改）
- 处理用户在两次扫描之间的外部操作

首版扫描完成后只保存基础快照数据（文件数、总大小、各模块摘要），不做对比展示。v2 在 Action Center 稳定后再做前后对比。

---

## 十二、全局公共能力

| 能力 | 说明 |
|------|------|
| 扫描入口 | 随时可从任意页面重新发起扫描 |
| 路径搜索 | 全局搜索文件名、路径、扩展名 |
| 过滤器 | 各模块均支持按大小、类型、时间过滤 |
| 风险提示 | 高风险操作前强制展示上下文说明 |
| 排除规则 | 全局生效，扫描前配置 |
| 在文件管理器中打开 | 任意文件/文件夹可直接在 Explorer 中定位 |

---

## 十三、交互设计原则

1. **首屏快**：扫描开始后 1 秒内 Overview Treemap 可见（部分结果），不等扫描完成
2. **问题优先**：Insight Panel 在 Overview 顶部，用户不需要逐页浏览也能知道先去哪里
3. **不要求技术知识**：不暴露 MFT、Hash、AppleDouble 等术语，用自然语言描述问题
4. **操作可预期**：每个危险操作前必须有"将要发生什么"的说明（文件列表预览）
5. **可回退**：所有执行动作默认走回收站，不提供永久删除
6. **联动**：Treemap、目录树、文件列表、详情面板始终保持选中状态同步

---

## 十四、首版 MVP 范围（收窄后）

### 必须做

| 模块 | 内容 |
|------|------|
| 扫描引擎 | NTFS MFT 极速 + 兼容模式降级，多文件夹会话，排除规则 |
| Overview | Treemap（文件+文件夹混合，面积=体积）+ 目录树 + 类型/年龄着色切换 + Insight Panel |
| Duplicates | 重复簇展示 + 保留策略 + 移到回收站 |
| Structure | 空文件夹（展示+删除）+ 其他诊断项（仅展示） |
| Residue | 内置规则 + 用户自定义规则 + 移到回收站 |
| File Types | 类型分布 + 年龄分布（合并在此模块） |
| Insight Panel | 问题摘要 + 跳转入口 |

### 明确不做（首版）

- Action Center（v2）
- 快照前后对比（v2）
- Structure 执行动作（除空文件夹外）
- 导出报告（v2）
- macOS / Linux 支持（v2/v3）
- 相似图片/音频检测（v2）
- 增量扫描（v2）

---

## 十五、已知问题与解决方案

### P1 — 边扫描边渲染的布局抖动

**问题**：Squarified Treemap 算法在新增节点时会全局重排，导致色块位置跳动。

**方案**：
- 扫描阶段：Treemap 每 2 秒批量刷新一次（而非每个文件触发），减少抖动感
- 扫描完成后：切换到 Squarified 算法做最终布局，加 300ms 动画过渡
- 扫描阶段目录树和数字实时更新，Treemap 接受轻微延迟

---

### P2 — Windows MFT 需要管理员权限

**问题**：MFT 直读需要 UAC 提权，普通用户可能拒绝。

**方案**：
- 应用启动时不强制提权
- 检测到 NTFS 盘时，提示"以管理员身份运行可启用极速模式（快约 10 倍）"
- 未提权时自动降级到兼容模式，功能不缺失
- 扫描模式标签明确显示当前模式

---

### P3 — 重复文件误判风险

**问题**：硬链接文件内容相同但不是"重复"；符号链接可能导致循环。

**方案**：
- 扫描时记录 inode，硬链接文件标记为"硬链接"，不纳入重复簇
- 符号链接不跟随（`follow_links: false`）
- 使用 BLAKE3 完整哈希确认，碰撞概率极低

---

### P4 — 残留规则误伤用户文件

**问题**：`*.tmp` 等规则可能误匹配用户有意保留的文件。

**方案**：
- 内置规则只包含高置信度模式
- 用户自定义规则默认风险等级"谨慎"，默认不勾选
- 执行前展示完整文件列表预览
- 支持"排除此文件"加入白名单

---

### P5 — 大目录下 Treemap 渲染性能

**问题**：百万级文件时，Canvas 渲染和 D3 布局计算可能卡顿。

**方案**：
- Treemap 只渲染当前层级（Drill-down 时才计算子层）
- 面积小于 2px × 2px 的节点跳过渲染
- 布局计算在 Web Worker 中执行，不阻塞主线程

---

### P6 — 跨盘重复检测计算量大

**问题**：多文件夹会话跨盘时，哈希计算量大幅增加。

**方案**：
- 跨盘重复检测默认关闭，用户可手动开启
- 开启时提示"跨盘检测耗时较长"
- 同盘内检测优先完成，跨盘作为后台任务

---

## 十六、技术栈确认（首版 Windows）

| 层 | 技术选型 |
|----|---------|
| 前端框架 | React 18 + TypeScript + Vite |
| 状态管理 | Zustand |
| 可视化 | D3-hierarchy（布局）+ Canvas 2D（渲染） |
| 桌面框架 | Tauri 2 |
| 后端语言 | Rust |
| 目录遍历 | windows-rs（MFT 极速）+ jwalk（兼容降级） |
| 哈希 | xxhash-rust（采样）+ blake3（完整） |
| 回收站 | trash crate |
| 持久化 | rusqlite（SQLite，bundled） |
| 并发 | tokio（Tauri 命令层）+ rayon（扫描/哈希） |
| 序列化 | serde + serde_json |
