# 文件夹透视产品需求文档（PRD）v1.0

## 1. 产品概述

- 产品暂定名：`Folder Insight` / `文件夹透视`
- 产品形态：Windows 桌面应用
- 首发平台：Windows 10 / Windows 11
- 首发对象：本地磁盘、外接磁盘、指定文件夹
- 扫描优先级：本地 `NTFS` 优先加速，`exFAT` / `FAT32` / 网络盘走兼容扫描模式

一句话定义：

让用户在几秒内看清一个文件夹"空间怎么被占用、结构哪里有问题、哪些内容可清理、如何安全处理"。

## 2. 背景与问题定义

用户处理文件夹问题时，常见痛点被拆散在多类工具中：

- 空间分析工具只回答"谁最大"
- 去重工具只回答"哪些重复"
- 文件搜索工具只回答"有哪些类型"
- 少有工具真正回答"这个文件夹整体健康吗、最该先处理什么"

本产品目标不是再做一个单点扫描器，而是构建统一工作流：

`透视 -> 诊断 -> 建议 -> 执行 -> 复盘`

## 3. 产品目标

- 让用户快速理解目录空间结构
- 自动发现高价值问题：重复文件、空目录、Mac 残留文件、单子目录链、深层嵌套、长路径等
- 将问题转化为可执行建议，并提供低风险批量处理
- 用扫描前后对比，让用户看见处理结果

## 4. 非目标

- 首版不做云同步盘深度集成
- 首版不做相似图片 / 相似视频识别
- 首版不做自动"智能整理到目标目录"的激进操作
- 首版不做企业管理端和多机协同

## 5. 核心用户

### 5.1 普通个人用户

典型场景：

- 下载盘、桌面、移动硬盘越来越乱
- 知道空间不够，但不知道先清什么
- 不愿手工在资源管理器里逐层翻找

核心诉求：

- 快速看清谁最占空间
- 找到明显可以处理的内容
- 在低风险前提下快速清理

### 5.2 重度内容用户

典型场景：

- 摄影、设计、开发、剪辑用户，文件量大且增长快
- 同一素材、导出、备份、副本分散在多个目录
- 文件夹嵌套深，归档习惯混乱

核心诉求：

- 看清目录结构和内容分布
- 快速识别重复和冗余
- 批量处理并保留可回退能力

### 5.3 技术用户

典型场景：

- 使用 NAS、外接盘、跨平台共享目录
- 经常遇到 `.DS_Store`、`._*`、重复备份、空目录
- 需要更细粒度的筛选、导出和规则控制

核心诉求：

- 兼顾速度和准确性
- 能识别跨平台残留与结构问题
- 对批量动作有清晰预览和审计能力

## 6. 核心价值主张

- 比空间工具更懂"结构"
- 比去重工具更懂"上下文"
- 比清理工具更安全
- 比报表工具更直接

## 7. 产品定位

产品应被定义为：

**文件夹透视与健康治理工具**

而不是：

- 单纯的磁盘空间分析器
- 单纯的重复文件删除器
- 单纯的缓存清理器

产品核心价值不在于提供多少独立功能，而在于将空间、重复、结构、残留统一成一个问题驱动、建议驱动、执行安全的治理流程。

## 8. 产品信息架构

一级导航建议如下：

- `Overview` 透视
- `Duplicates` 重复
- `Structure` 结构
- `Residue` 残留
- `Action Center` 处理中心
- `Reports` 报告与对比

贯穿全局的公共能力：

- 扫描入口
- 路径搜索
- 过滤器
- 选中项批量加入处理中心
- 风险提示
- 扫描快照切换

## 9. 功能模块详细需求

### 9.1 扫描与索引引擎

#### 9.1.1 模块目标

快速得到完整目录画像，并为后续分析模块提供统一、可复用的数据基础。

#### 9.1.2 功能要求

- 用户可选择扫描整个盘符或指定文件夹
- 本地 `NTFS` 扫描优先使用 `MFT` 加速
- 非 `NTFS`、网络盘、权限受限路径自动回退到兼容递归扫描
- 扫描结果需记录以下字段：
  - 完整路径
  - 父级路径
  - 文件名
  - 类型（文件 / 文件夹）
  - 扩展名
  - 逻辑大小
  - 实际占用大小
  - 创建时间
  - 修改时间
  - 访问时间（如可用）
  - 文件属性
  - 隐藏 / 系统标记
  - 硬链接信息
  - 文件系统类型
- 扫描中实时显示进度、已发现文件数、累计大小、当前阶段
- 扫描完成后将结果写入本地索引，以支持后续模块秒开
- 用户可取消扫描
- 扫描取消后可选择保留当前阶段结果或直接放弃

#### 9.1.3 关键设计

- 首次体验必须足够快，特别是在本地 `NTFS` 环境下
- 即使扫描未完全结束，界面也应尽早提供可浏览的部分结果
- UI 明确提示扫描模式：
  - `极速模式（NTFS）`
  - `兼容模式（其他文件系统 / 网络盘）`

#### 9.1.4 验收标准

- 扫描大目录时，用户能在结果完全完成前开始浏览
- 扫描模式切换对用户透明，不要求用户手动配置
- 索引可复用于重复分析、结构分析、残留识别等模块

### 9.2 Overview 透视页

#### 9.2.1 模块目标

让用户第一眼看清空间占用结构和热点区域。

#### 9.2.2 核心能力

- 提供类似 `SpaceSniffer` / `WizTree` 的矩形树图视图
- 每个区块按占用大小显示
- 颜色按文件类型或目录类别区分
- 左侧提供目录树和大小列表
- 支持按大小、文件数、修改时间排序
- 顶部提供概览指标：
  - 总大小
  - 文件数
  - 文件夹数
  - 最大目录
  - 最大文件
  - 最近新增大文件
  - 可疑问题数量
- 提供文件类型分布视图，按类别显示体积和数量
- 点击任意区块可联动定位到目录树、文件列表和右侧详情
- 支持搜索路径、文件名、扩展名
- 支持多级 Drill-down：从盘到目录到子目录到文件

#### 9.2.3 右侧详情面板

建议显示：

- 当前节点路径
- 直接子项数 / 累计子项数
- 占用大小
- 文件类型构成
- 最近变更
- 是否命中问题规则
- 是否已加入处理中心

#### 9.2.4 交互原则

- 可视化视图和表格视图始终联动
- 用户从任何节点都能快速"放大"和"回到上级"
- 透视页本身不仅是浏览器，也是后续问题发现入口

#### 9.2.5 验收标准

- 用户在透视页内无需切页即可定位最占空间的目录和最大的文件类型
- 对大目录的浏览操作应流畅可用

### 9.3 Duplicates 重复文件模块

#### 9.3.1 模块目标

准确、快速地找出可处理的重复内容，并以低风险方式批量处理。

#### 9.3.2 核心流程

默认流程：

`文件大小分组 -> Quick Hash -> Full Hash -> 重复簇生成 -> 保留建议 -> 执行`

#### 9.3.3 功能要求

- 默认按文件大小先做第一轮分组
- 小文件可直接进入 Full Hash
- 大文件优先做 Quick Hash 以提升速度
- Quick Hash 默认取头部 / 中部 / 尾部固定块
- Full Hash 用于最终确认文件内容一致
- 重复结果按"重复簇"展示，而不是只展示散文件
- 支持以下过滤条件：
  - 目录范围
  - 文件类型
  - 最小文件大小
  - 最近修改时间
  - 是否跨目录
- 支持区分：
  - 同目录重复
  - 跨目录重复
  - 备份式重复
- 每个重复簇显示：
  - 文件数量
  - 总占用
  - 可回收空间
  - 涉及目录
  - 推荐保留项
- 用户可选择保留策略：
  - 保留最新
  - 保留最旧
  - 保留指定目录优先
  - 保留原路径优先
  - 手工指定
- 执行动作前提供影响预览

#### 9.3.4 风险控制

- 系统目录、应用目录、隐藏目录默认不自动勾选
- 若重复簇内存在权限异常、只读文件、硬链接、路径冲突，需标记风险
- 默认动作应为"移到回收站"或"移到隔离区"

#### 9.3.5 验收标准

- 同内容不同路径文件能稳定归组
- 用户能通过规则快速选出每组待删除副本
- 重复模块输出结果可直接流转至处理中心

### 9.4 Structure 结构诊断模块

#### 9.4.1 模块目标

发现目录结构层面的低效与异常，让用户看到"结构问题"与"空间问题"的区别。

#### 9.4.2 功能点一：空文件夹

功能要求：

- 识别完全空目录
- 区分以下状态：
  - 当前已空
  - 清理候选项处理后将变空
- 支持批量选中并删除到回收站

交互要求：

- 展示父目录上下文，帮助用户理解这些空目录是否成片出现

#### 9.4.3 功能点二：单子目录链

定义：

连续只包含一个直接子目录的目录链，例如：

`A/B/C/D/file.txt`

其中 `A -> B -> C -> D` 之间长期只有单一路径延伸。

功能要求：

- 识别连续单子目录链
- 显示链长度
- 显示末端文件数和文件夹数
- 预估扁平化后的路径变化
- 若存在命名冲突或语义目录名，标记为需人工确认

建议动作：

- 扁平化到上级目录
- 合并到最近有内容的父级
- 忽略该条建议

#### 9.4.4 功能点三：深层嵌套

功能要求：

- 识别目录深度超过阈值的路径
- 支持按深度阈值排序查看
- 提示可能带来的查找困难和管理成本

#### 9.4.5 功能点四：超长路径

功能要求：

- 标记路径长度接近或超过 Windows 常见兼容阈值的项目
- 支持查看完整路径和父级上下文
- 支持加入处理中心，后续通过移动 / 扁平化等方式处理

#### 9.4.6 功能点五：小文件过密

功能要求：

- 识别文件数量极多但总体体积不大的目录
- 用于发现缓存、构建产物、日志碎片等目录

#### 9.4.7 功能点六：异常命名 / 冗余层级

功能要求：

- 识别低信息量目录链，例如：
  - `A/A/A`
  - `New Folder/New Folder`
  - `副本/复制/Copy`
- 首版使用轻规则，不做高风险自动判断

#### 9.4.8 验收标准

- 用户能清楚理解每一类结构问题的成因、风险和建议动作
- 每条结构问题都必须带有路径上下文和处理建议

### 9.5 Residue 残留文件模块

#### 9.5.1 模块目标

识别并处理跨平台和系统级残留垃圾文件。

#### 9.5.2 首版识别范围

- macOS：
  - `._*`
  - `.DS_Store`
  - `AppleDouble`
- Windows：
  - `Thumbs.db`
  - `desktop.ini`
- 常见临时残留：
  - `.tmp`
  - 部分下载残片
  - 通用缓存规则命中项

#### 9.5.3 功能要求

- 自动按规则归类，不与普通文件混在一起
- 对每类残留显示来源说明，例如"该文件通常由 macOS Finder 生成"
- 支持一键清理到回收站或隔离区
- 对系统关键文件夹中的同名文件谨慎处理，必要时默认不勾选

#### 9.5.4 验收标准

- 用户可在单独视图里一次性处理跨平台残留
- 不需要用户自行编写通配规则

### 9.6 File Types 文件类型与分类模块

#### 9.6.1 模块目标

让用户按内容类型治理，而不是只按扩展名查找。

#### 9.6.2 功能要求

- 根据扩展名建立大类，包括但不限于：
  - 图片
  - 视频
  - 音频
  - 文档
  - 压缩包
  - 安装包
  - 代码
  - 数据库
  - 缓存
  - 未知类型
- 支持扩展名映射和自定义分类
- 支持查看每类文件的：
  - 体积
  - 数量
  - 最大文件
  - 主要分布目录
- 支持按类型进入批量操作流
- 支持"未知类型"视图，帮助发现异常文件

#### 9.6.3 动作要求

- 第一版不提供按类型直接永久删除
- 第一版改为"加入处理列表"，由 `Action Center` 统一执行

#### 9.6.4 验收标准

- 用户能快速回答"这个目录主要是什么类型的东西占空间"

### 9.7 Recommendation Engine 建议引擎

#### 9.7.1 模块目标

把扫描结果和分析结果转换成用户能执行的计划。

#### 9.7.2 输出形式

- 问题卡片
- 优先级排序
- 可回收空间估算
- 风险等级
- 建议动作

#### 9.7.3 示例建议

- 发现 12.4 GB 完全重复文件，建议保留最新版本并移除其余副本
- 发现 186 个空文件夹，建议批量移除
- 发现 9 条单子目录链，预计可减少 27 层冗余层级
- 发现 3.2 GB Mac 残留文件，建议一键清理

#### 9.7.4 排序逻辑

建议综合以下因子排序：

- 可节省空间
- 影响范围
- 操作风险
- 用户可理解度
- 处理收益可视化程度

#### 9.7.5 验收标准

- 用户无需手动浏览所有视图，也能知道先处理哪几件事最值

### 9.8 Action Center 处理中心

#### 9.8.1 模块目标

将所有待处理项统一管理，并保证批量执行安全、可解释、可回退。

#### 9.8.2 功能要求

- 所有模块结果都可加入处理列表
- 处理前统一展示：
  - 目标项数量
  - 释放空间估算
  - 可能风险
- 支持模拟执行
- 支持以下动作：
  - 移到回收站
  - 移到隔离区
  - 移动
  - 扁平化
- 默认不提供永久删除；如提供，必须二次确认且默认关闭
- 对移动和扁平化操作，需要预览冲突路径
- 执行前重新验证目标是否已变化
- 生成操作日志，支持撤销可撤销项

#### 9.8.3 安全原则

- 默认保守
- 默认可回退
- 默认不碰系统目录
- 默认不过度自动化

#### 9.8.4 验收标准

- 用户对批量处理有足够信任，不会因误删风险而放弃使用

### 9.9 Reports 报告与前后对比

#### 9.9.1 模块目标

让用户看到处理收益，并支持复盘。

#### 9.9.2 功能要求

- 保存扫描快照
- 展示处理前后变化：
  - 释放空间
  - 删除文件数
  - 删除空目录数
  - 减少层级数
  - 重复簇处理数
- 支持导出简洁报告
- 支持对比两次扫描结果

#### 9.9.3 验收标准

- 用户清理完成后能得到一页摘要，而不是处理完就结束

## 10. 关键用户流程

### 10.1 首次扫描流程

1. 用户选择目录并开始扫描
2. 系统自动识别扫描模式
3. 扫描过程中逐步展示可用结果
4. 用户进入 `Overview` 看到整体空间分布

### 10.2 问题处理流程

1. 建议引擎给出优先处理项
2. 用户进入 `Duplicates` / `Structure` / `Residue` 查看详情
3. 用户将候选项加入 `Action Center`
4. 用户先做模拟执行，再真正执行
5. 执行完成后进入 `Reports` 查看收益

## 11. 关键交互原则

- 首屏必须快且清楚
- 所有危险操作都必须有上下文说明
- 分析结果应从"文件视角"提升到"问题视角"
- 不要求用户理解 `MFT`、`Hash`、`AppleDouble` 等技术词也能完成操作
- 视觉上应优先突出：
  - 最大问题
  - 建议动作
  - 风险等级

## 12. 非功能需求

### 12.1 性能

- 本地 `NTFS` 盘应体现显著速度优势
- 对超大目录应支持渐进式展示

### 12.2 兼容性

- 非 `NTFS` 模式下功能不缺失，只是扫描方式不同
- 对移动硬盘、外接盘、常见网络路径保持兼容

### 12.3 稳定性

- 超大目录扫描不中断或崩溃
- 支持中断取消
- 支持结果复用

### 12.4 准确性

- 目录大小、重复归组、空目录判断必须可解释
- 对高风险操作必须提供复核机制

### 12.5 隐私

- 所有扫描和分析默认本地完成
- 默认不上传文件内容

## 13. 技术方案建议

### 13.1 扫描层

- `NTFS MFT` 读取
- 通用递归扫描双引擎

### 13.2 索引层

- 本地轻量数据库保存条目、分类、规则命中结果

### 13.3 分析层

- 重复分析器
- 结构规则引擎
- 残留识别器
- 建议排序器

### 13.4 执行层

- 回收站
- 隔离区
- 移动
- 扁平化
- 撤销日志

### 13.5 UI 层

- 透视图
- 列表
- 详情面板
- 处理中心
- 报告视图

## 14. MVP 范围

### 14.1 必须做

- `Overview` 透视页
- `Duplicates` 重复文件
- `Structure` 中的：
  - 空目录
  - 单子目录链
  - 深层嵌套
  - 长路径
- `Residue` 中的：
  - `.DS_Store`
  - `._*`
  - `Thumbs.db`
  - `desktop.ini`
- `Action Center` 中的：
  - 模拟执行
  - 回收站
  - 隔离区
- `Reports` 的前后对比

### 14.2 后续版本

- 相似图片 / 相似音频
- 增量扫描
- 目录对目录比对
- 增长趋势分析
- 自动化规则和计划任务

## 15. 成功指标

- 扫描完成率
- 首次扫描到首次执行动作的转化率
- 平均释放空间
- 平均处理问题数
- 撤销率
- 用户二次打开率
- 建议动作被采纳的比例

## 16. 风险与决策点

- `MFT` 加速只能覆盖 `NTFS`，本地快但外部介质和网络盘未必快
- 去重结果必须极其保守，否则用户信任会断裂
- 单子目录链扁平化必须解决命名冲突和回退
- 残留文件规则不能误伤用户真实文件
- 处理中心若没有强约束，容易演化成高风险"批量删除器"

## 17. 版本结论

这款产品的差异化不在于"有多少功能"，而在于把空间、重复、结构、残留统一成一个可执行的健康治理流程。

最终，产品应让用户得到的不是一堆图，而是一句明确结论：

"这个文件夹有哪些问题，先处理哪几项，能释放多少空间，会带来什么风险。"
